<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="dms.aut.ac.nz.dmsandroid.MainActivity">

    <Button
        android:id="@+id/CheckBluetoothSupport"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Check Bluetooth"
        android:onClick="checkBluetoothSupport" />

    <Button
        android:id="@+id/EnableBluetoothSupport"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enable Bluetooth"
        android:onClick="enableBluetoothSupport"
        android:layout_below="@+id/CheckBluetoothSupport" />
    <Button
        android:id="@+id/QueryPairedDevices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Query Paired Devices"
        android:onClick="queryPairedDevices"
        android:layout_below="@+id/EnableBluetoothSupport" />
    <Button
        android:id="@+id/DiscoverableDevice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Make Device Discoverable"
        android:onClick="makeDeviceDiscoverable"
        android:layout_below="@+id/QueryPairedDevices" />
    <ListView
        android:id="@+id/pairedDevicesList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/DiscoverableDevice" />
</RelativeLayout>
